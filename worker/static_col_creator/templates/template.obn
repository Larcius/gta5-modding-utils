Version 43 31
{
	Type BoundComposite
	Radius ${BSPHERE.RADIUS}
	AABBMax ${BBOX.MAX.X} ${BBOX.MAX.Y} ${BBOX.MAX.Z}
	AABBMin ${BBOX.MIN.X} ${BBOX.MIN.Y} ${BBOX.MIN.Z}
	Centroid ${BSPHERE.CENTER.X} ${BSPHERE.CENTER.Y} ${BSPHERE.CENTER.Z}
	CG ${BSPHERE.CENTER.X} ${BSPHERE.CENTER.Y} ${BSPHERE.CENTER.Z}
	Children ${NUM_CHILDREN}
	{
${CHILDREN.BOUNDS}
	}
	ChildTransforms ${NUM_CHILDREN}
	{
${CHILD_TRANSFORMS.MATRICES}
	}
	ChildFlags ${NUM_CHILDREN}
	{
${CHILD_FLAGS.ITEMS}
	}
}
